import React from "react";

import Navigation from "./Navigation.js";

const Header = (props) => {
  return (
    <React.Fragment>
      <Navigation />
    </React.Fragment>
  );
};

export default Header;
